{% extends '@layout/centered.html.twig' %}

{% set title = "Login" %}

{% block content %}
    {{ form(form) }}
    {% if magickLink %}
        <p>You should receive this logging link in your mailbox (it's password less authentication), but emailing is not yet implemented...<br /> <a href="{{ magickLink }}">{{ magickLink }}</a></p>
    {%  endif %}
{% endblock %}